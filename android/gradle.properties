org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Gradle daemon to prevent cache file locking conflicts
org.gradle.daemon=false
# Disable Kotlin incremental compilation to prevent .tab file corruption
kotlin.incremental=false
# Disable parallel execution to avoid concurrent cache access
org.gradle.parallel=false
# Disable configuration cache to avoid stale cache issues
org.gradle.configuration-cache=false

